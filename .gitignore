# Build artifacts / packaged extension (regenerate with `zip -r serp-snapshot.zip serp-snapshot`)
serp-snapshot.zip
*.zip

# Generated Web Store submission bundle (graphics + zip; regenerate any time)
submission/

# Node / test scratch (tests run from the session scratchpad, not the repo)
node_modules/

# OS cruft
.DS_Store
